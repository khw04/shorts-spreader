export const metadata = {
  title: 'Shorts Spreader',
  description: 'Bootstrap workspace for the Shorts Spreader MVP.'
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
